# Copy to .env for local testing (bites_mcp reads these at import time).
# In a real MCP client config, set these as that server's env vars instead.

# Where your Bites app is deployed
BITES_URL=https://your-app.vercel.app

# Must match the API_KEY environment variable set on that Bites deployment
BITES_API_KEY=change-me
